/*
    From constraints we cas say that solution should be brute force
    just normally create subsets and add them 
    tc --> 2^n => n = 20, => 2^20
    with DP --> subset sum problem --> tc -> n*maxsum, in this case is will be higher than 2^n
*/